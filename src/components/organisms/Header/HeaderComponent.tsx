import { Box } from "@mui/material";
import { FC } from "react";

export const HeaderComponent: FC = () => {
  return (
    <Box height={94} borderBottom="1px solid" borderColor="divider">
      Header
    </Box>
  );
};

import { createTheme } from "@mui/material";

const palette = {
  text: {
    primary: "#4B5675",
    secondary: "#071437",
  },
  divider: "#E4E6EF",
};

export const theme = createTheme({ palette });
